{
  "name": "data-picker",
  "version": "1.0.0",
  "description": "a data picker demo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "stylus:watch": "mkdir css & stylus -w -m styl/ -o css/"
  },
  "keywords": [
    "component",
    "calendar"
  ],
  "author": "pengsanchun",
  "license": "ISC",
  "dependencies": {
    "colors.css": "^2.3.0",
    "normalize.css": "^7.0.0"
  }
}
